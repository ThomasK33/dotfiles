Run the GitHub triage convergence loop for `{{REPO}}` using the global `issue-triage-loop` skill and repo-local `.mux/triage-loop/` state.

Heartbeat goal discipline: if this message was delivered by a scheduled heartbeat, first read actual state. If there is eligible, active, terminal, or ledgered triage work, call `set_goal` once with a bounded objective like `Converge {{REPO}} triage loop by repeatedly running bounded reconciliation cycles until no eligible {{ENTRY_LABEL}} issues, active/terminal triage workspaces, unarchived triage_done records, or attention_required ledger records remain.` Use a finite turn cap (for example 20) and do not replace an already-active goal. If goal tooling is unavailable or refuses because a goal already exists, continue with one bounded reconciliation cycle and report that state. Do not set a new goal for background child terminal wake-up messages; terminal wake-ups should retrieve those reports and continue the current convergence work.

Before side effects, read `.mux/triage-loop/README.md`, `.mux/triage-loop/child-prompt-template.md`, and `.mux/triage-loop/ledger.json` if present.

Desired state: every open issue with `{{ENTRY_LABEL}}` and without `{{IN_PROGRESS_LABEL}}`/`{{DONE_LABEL}}` has at most one non-disposable Mux workspace launched for triage. Maintain at most 5 active triage workspaces at a time; each scheduling cycle may launch only the remaining capacity after counting queued/starting/running/awaiting_report/pending/backgrounded triage workspaces. Every terminal triage workspace should be reconciled from GitHub state, have any child `### Parent ledger hints` ingested if available, and be archived after the issue has reached `{{DONE_LABEL}}`, a GitHub issue type has been set, a triage report has been posted or confirmed, and no active task remains for the workspace.

Actual-state reads: if commands fail with a `mise` trust error, run `mise trust` once from the repository root, then use `gh issue list --repo {{REPO}} --state open --label {{ENTRY_LABEL}} --json number,title,url,createdAt,labels,issueType --limit 200` to find candidate issues; use `task_list` to inspect existing descendant tasks/workspaces; use the local ledger as an additional duplicate, hint, recovery, and terminal-state guard. If `.mux/triage-loop/triagectl.py` is present, use it for deterministic ledger summaries, launch planning, hint parsing, terminal verification, archival planning, and pruning; keep Mux workspace/GitHub side effects agent-mediated.

Backgrounding/workspace monitor discipline: follow the `background-monitors` skill. Launch child and retry workspaces with `run_in_background: true` so this parent workspace is woken when they finish; do not await them immediately. Use `task_list` plus ledger/GitHub state on later ticks, goal continuations, or terminal wake-ups to reconcile results, and call `task_await` only when the current parent decision actually depends on a completed child's report. After launching children, stop the parent turn if no terminal reports currently need reconciliation and wait for terminal wake-ups; do not poll or busy-wait.

Launch guards: exclude any issue with `{{DONE_LABEL}}`, `{{IN_PROGRESS_LABEL}}`, an existing ledger record, or an existing task/workspace for idempotency key `github-triage:{{REPO}}#<number>`. Maintain at most 5 active triage workspaces; launch at most the remaining capacity in each scheduling cycle. Sort oldest issue first, with only soft priority boosts for already-eligible same-repo prerequisite/foundational issues found in prior ledger relationship/readiness hints. Do not auto-label related issues and do not launch cross-repo work.

For each eligible issue to launch: create a full workspace (`kind: workspace`, `workspace.mode: new`, non-disposable) with title `Triage #<number>` and branch `triage-<number>`; do not include owner/repo in either because Mux already separates workspaces by project. Prompt it using `.mux/triage-loop/child-prompt-template.md` with placeholders filled in. After and only after the workspace starts successfully, update `.mux/triage-loop/ledger.json`, then apply `{{IN_PROGRESS_LABEL}}` to the issue. If labeling fails after workspace creation, keep the ledger record and report attention-needed rather than launching a duplicate.

Recovery guard: a ledgered issue with terminal/failed/ambiguous prior work may receive one normal retry workspace only when no active workspace exists for the same issue and the ledger has not already recorded `recoveryAttempted: true`. If the retry does not converge, mark/report `attention_required` and wait for human action.

Terminal reconciliation: for completed/reported child workspaces, parse the child final response's `### Parent ledger hints` JSON if present and store compact optional hints in the ledger. Missing/malformed hints are non-fatal. Verify transition labels, `issueType.name`, and the Mux triage report comment from GitHub before marking `lastObservedState: "triage_done"`. Do not require or set bug/feature/chore/tech-debt labels.

Archival pass: identify descendant triage workspaces whose ledger record has `lastObservedState: "triage_done"`, whose issue type has been observed, and whose workspace has no queued/starting/running/awaiting_report/pending/backgrounded work. Archive those workspaces with `task_workspace_lifecycle(action="archive")`. Do not archive non-terminal, attention-required, or active workspaces. If archival requires confirmation for untracked paths or otherwise fails, do not delete anything; report the workspace and blocker as attention-needed. Do not use `delete_worktree` or `remove` unless explicitly asked.

Progress guard: if a cycle makes no progress—no launch, terminal reconciliation, archive, retry, or attention-required classification—do not loop recursively. Report the current state and wait for a heartbeat, terminal wake-up, or human action.

Report launched workspaces, ingested hints, archived workspaces, convergence, and blockers in the parent workspace.
